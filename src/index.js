import './scss/style.scss';
import { dataTreks } from './utils';

const root = document.getElementById('app');
const audioplayer = document.createElement(div);
audioplayer.classList.add('wr');

root.append(audioplayer);
